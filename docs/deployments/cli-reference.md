<!-- Generated by tools/hub-generate.go from `gitea-deployments --help`. Do not edit. -->
# gitea-deployments command reference

## gitea-deployments

```
gitea-deployments — a thin client over /api/deployments/v1.

Usage: gitea-deployments <command> [positional...] [flags]

Commands:
  approvals                 List held deploys and their approval state
  approve                   Approve a held deploy
  audit                     List delivery audit events across every repository the caller can see
  deploy                    Plan or dispatch a deploy of a release to an environment
  deployment-summary        Deployment summary view
  deployments               List deployments across every repository the caller can see
  environment               Read one environment by id
  environment-create        Create an environment
  environment-delete        Delete an environment
  environment-update        Update an environment
  environments              List environments across every repository the caller can see
  grid                      The release × environment grid
  overview                  The cross-repository CI summary and its comparison window
  overview-repos            List repositories by run volume, with success rate and average duration
  overview-trends           The daily trend series: total, successful and failed runs, average duration and deployments
  reject                    Reject a held deploy
  releases                  List a repository's releases, the rows of the grid
  repo-environment          Read one environment
  repo-environment-secrets  List the secret NAMES scoped to one environment
  repo-environments         List one repository's environments
  repos                     List the repositories the caller can see delivery data for
  rollback                  Plan or dispatch a deploy of a release to an environment
  runs                      List Actions runs across every repository the caller can see
  secret-scope-create       Bind a secret to an environment
  secret-scope-delete       Unbind a secret from an environment
  workflows                 List workflows with their run counts, success rate and average duration

Every command accepts the section I query grammar as flags:
  --filter 'field[op]=value'  repeatable; sent to the server, never applied locally
  -q, --sort-by, --order      search and sort
  --limit, --offset, --cursor pagination
  --expand                    sub-resources, one level deep
  --json                      the API response verbatim and unshaped

Credentials resolve as: --token, then $GITEA_DEPLOYMENTS_TOKEN or $FORGE_TOKEN or $GITEA_TOKEN.
Exit codes: 0 success, 1 request failed, 2 usage error, 3 refused by the server.
See docs/deployments/openapi.json for the published OpenAPI document.
```

## gitea-deployments approvals

```
gitea-deployments approvals — List held deploys and their approval state

  GET /approvals

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments approve

```
gitea-deployments approve — Approve a held deploy

  POST /approvals/{id}/approve

Positional arguments: id

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments audit

```
gitea-deployments audit — List delivery audit events across every repository the caller can see

  GET /audit

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments deploy

```
gitea-deployments deploy — Plan or dispatch a deploy of a release to an environment

  POST /deployments

Flags:
  -confirm
    	False, the default, returns the deploy plan and dispatches nothing — the first of the two confirm steps. True dispatches.
  -cursor string
    	opaque cursor from a previous response
  -environment string
    	required. Target environment, for example prod.
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -override-reason string
    	Why the environment's sequence rule is being bypassed. Required when the deploy plan reports outcome "override"; it is written to the audit log.
  -q string
    	free-text search
  -release-tag string
    	required. Release tag to deploy. A prior tag is a rollback; it is the same request.
  -repo string
    	required. Target repository as owner/name.
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments deployment-summary

```
gitea-deployments deployment-summary — Deployment summary view

  GET /deployment-summary

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments deployments

```
gitea-deployments deployments — List deployments across every repository the caller can see

  GET /deployments

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments environment

```
gitea-deployments environment — Read one environment by id

  GET /environments/{id}

Positional arguments: id

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments environment-create

```
gitea-deployments environment-create — Create an environment

  POST /environments

Flags:
  -admins-can-bypass
    	Let a repo admin bypass the gate. Defaults to true on create; an update that omits it leaves the existing value unchanged.
  -cursor string
    	opaque cursor from a previous response
  -depends-on string
    	Environments a release must pass through first.
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -name string
    	required. Environment name.
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -releases-only
    	Refuse prereleases; this environment takes finished releases only.
  -repo-id string
    	Repository id; 0 is the instance-wide default set.
  -require-prior-deployment
    	Gate when a dependency hasn't held the release.
  -required-reviewers string
    	Reviews needed. Defaults to 1.
  -restrict-reviewers
    	Enable bypass allowlist.
  -review-policy string
    	Review policy. Defaults to none.
  -reviewer-team-ids string
    	Team IDs allowed to bypass.
  -reviewer-user-ids string
    	User IDs allowed to bypass.
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -sort-order string
    	Render order.
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments environment-delete

```
gitea-deployments environment-delete — Delete an environment

  DELETE /environments/{id}

Positional arguments: id

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments environment-update

```
gitea-deployments environment-update — Update an environment

  PUT /environments/{id}

Positional arguments: id

Flags:
  -admins-can-bypass
    	Let a repo admin bypass the gate. Defaults to true on create; an update that omits it leaves the existing value unchanged.
  -cursor string
    	opaque cursor from a previous response
  -depends-on string
    	Environments a release must pass through first.
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -name string
    	required. Environment name.
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -releases-only
    	Refuse prereleases; this environment takes finished releases only.
  -repo-id string
    	Repository id; 0 is the instance-wide default set.
  -require-prior-deployment
    	Gate when a dependency hasn't held the release.
  -required-reviewers string
    	Reviews needed. Defaults to 1.
  -restrict-reviewers
    	Enable bypass allowlist.
  -review-policy string
    	Review policy. Defaults to none.
  -reviewer-team-ids string
    	Team IDs allowed to bypass.
  -reviewer-user-ids string
    	User IDs allowed to bypass.
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -sort-order string
    	Render order.
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments environments

```
gitea-deployments environments — List environments across every repository the caller can see

  GET /environments

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments grid

```
gitea-deployments grid — The release × environment grid

  GET /grid

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments overview

```
gitea-deployments overview — The cross-repository CI summary and its comparison window

  GET /overview

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments overview-repos

```
gitea-deployments overview-repos — List repositories by run volume, with success rate and average duration

  GET /overview/repos

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments overview-trends

```
gitea-deployments overview-trends — The daily trend series: total, successful and failed runs, average duration and deployments

  GET /overview/trends

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments reject

```
gitea-deployments reject — Reject a held deploy

  POST /approvals/{id}/reject

Positional arguments: id

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments releases

```
gitea-deployments releases — List a repository's releases, the rows of the grid

  GET /repos/{owner}/{repo}/releases

Positional arguments: owner repo

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments repo-environment

```
gitea-deployments repo-environment — Read one environment

  GET /repos/{owner}/{repo}/environments/{name}

Positional arguments: owner repo name

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments repo-environment-secrets

```
gitea-deployments repo-environment-secrets — List the secret NAMES scoped to one environment

  GET /repos/{owner}/{repo}/environments/{name}/secrets

Positional arguments: owner repo name

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments repo-environments

```
gitea-deployments repo-environments — List one repository's environments

  GET /repos/{owner}/{repo}/environments

Positional arguments: owner repo

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments repos

```
gitea-deployments repos — List the repositories the caller can see delivery data for

  GET /repos

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments rollback

```
gitea-deployments rollback — Plan or dispatch a deploy of a release to an environment

  POST /deployments

Flags:
  -confirm
    	False, the default, returns the deploy plan and dispatches nothing — the first of the two confirm steps. True dispatches.
  -cursor string
    	opaque cursor from a previous response
  -environment string
    	required. Target environment, for example prod.
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -override-reason string
    	Why the environment's sequence rule is being bypassed. Required when the deploy plan reports outcome "override"; it is written to the audit log.
  -q string
    	free-text search
  -release-tag string
    	required. Release tag to deploy. A prior tag is a rollback; it is the same request.
  -repo string
    	required. Target repository as owner/name.
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments runs

```
gitea-deployments runs — List Actions runs across every repository the caller can see

  GET /runs

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments secret-scope-create

```
gitea-deployments secret-scope-create — Bind a secret to an environment

  POST /secret-scopes

Flags:
  -cursor string
    	opaque cursor from a previous response
  -environment string
    	required. Environment to scope the secret to.
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -repo-id string
    	required. Repository id.
  -secret-name string
    	required. Secret name as it appears in Gitea's Actions secrets.
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments secret-scope-delete

```
gitea-deployments secret-scope-delete — Unbind a secret from an environment

  DELETE /secret-scopes/{id}

Positional arguments: id

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```

## gitea-deployments workflows

```
gitea-deployments workflows — List workflows with their run counts, success rate and average duration

  GET /workflows

Flags:
  -cursor string
    	opaque cursor from a previous response
  -expand string
    	comma-separated sub-resources, one level deep
  -filter field[op]=value
    	repeatable field[op]=value filter; sent to the server verbatim
  -json
    	emit the API response verbatim and unshaped
  -limit int
    	page size; the server defaults to 50 and caps at 200
  -offset int
    	row offset, converted to the 1-based page the API takes
  -order string
    	asc or desc
  -q string
    	free-text search
  -server string
    	Gitea base URL; defaults to $GITEA_DEPLOYMENTS_SERVER or $GITEA_SERVER or $FORGE_HOST
  -sort-by string
    	sort field
  -token string
    	API token; resolved by the same precedence detect.sh implements
```
